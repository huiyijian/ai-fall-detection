# 🎯 交互式老人跌倒检测与AI急救助手 - MVP 开发计划 (v4.0)

> **核心原则**: **奥卡姆剃刀 (Occam's Razor)** - 只保留核心功能，去除一切非必要内容。
>
> **目标**: 在 1 小时内实现一个**一定能跑通**的最小可行版本。

---

## 1. MVP 核心功能清单 (Must-Have)

| 功能 | 状态 | 优先级 | 说明 |
|------|------|--------|------|
| 视频流模拟（本地视频循环播放） | ✅ 已完成 | P0 | 模拟实时摄像头，支持 `q` 退出 |
| YOLOv8-Pose 跌倒检测 | ✅ 已完成 | P0 | 宽高比 + 躯干角度判定 |
| 时序滤波（连续 N 帧确认） | ✅ 已完成 | P0 | 减少误报 |
| VLM (QVQ) 急救建议生成 | ✅ 已完成 | P0 | 场景化 Prompt，JSON 输出 |
| TTS 女性语音播报 | ⚠️ 需调整 | P0 | 使用女性声音，更亲和 |
| 冷却机制 | ✅ 已完成 | P0 | 30 秒防重复报警 |
| 键盘控制（暂停/退出） | ✅ 已完成 | P0 | `q` 退出, `s` 暂停 |

---

## 2. 非核心功能（后续迭代）(Nice-to-Have)

以下功能**暂不实现**，待 MVP 验证后再逐步添加：

### 2.1 语音识别 (STT)
- **计划**: 本地部署 Whisper tiny 模型（约 40MB）
- **原因**: 云端 API 延迟高、不稳定；本地模型轻量、快速
- **技术**: `openai-whisper` (tiny 版本)

### 2.2 多帧上传 + 跌倒速度分析
- **计划**: 上传 2-3 张连续帧 + 计算跌倒速度（像素/秒）
- **原因**: 单帧信息有限，多帧 + 速度能更准确判断伤情
- **实现**:
  ```python
  # 计算重心变化速度
  center_current = (x1 + x2) / 2, (y1 + y2) / 2
  fall_speed = abs(center_current - center_previous) / dt
  ```

### 2.3 YOLOv8 CUDA 加速
- **计划**: 使用最新版 `ultralytics` + `torch` CUDA 推理
- **原因**: NVIDIA GPU 加速可显著提升 FPS（从 15 → 60+）
- **技术栈**:
  ```bash
  pip install ultralytics torch torchvision --index-url https://download.pytorch.org/whl/cu121
  ```
- **PPT 展示点**: 
  - CPU vs GPU 对比 FPS
  - FP16 半精度加速
  - TensorRT 加速（可选）

### 2.4 卡尔曼滤波 (Kalman Filter)
- **计划**: 对关键点坐标进行平滑滤波
- **原因**: YOLO 检测结果可能有抖动，卡尔曼滤波能平滑轨迹
- **技术**: `filterpy` 库或手写简单实现
- **PPT 展示点**: 
  - 原始轨迹 vs 滤波后轨迹对比
  - 噪声抑制效果

### 2.5 Gradio 可视化 Dashboard
- **计划**: 使用 Gradio 创建 Web 界面展示检测结果
- **原因**: 领导/老师喜欢看图表和可视化
- **功能**:
  - 实时视频流显示
  - 跌倒历史记录（时间、严重程度、建议）
  - 时序分析图（宽高比变化曲线）
  - FPS 监控
- **技术**: `gradio` 库

### 2.6 实时摄像头支持
- **计划**: 支持从 USB 摄像头实时读取
- **原因**: 真实场景需要实时检测
- **实现**: 替换 `cv2.VideoCapture(0)` 为 `cv2.VideoCapture(video_path)`
- **当前状态**: 代码已注释，后续取消注释即可

---

## 3. MVP 开发步骤（按优先级）

### Step 1: 确认核心功能已跑通 ✅
- [x] YOLOv8-Pose 检测
- [x] 跌倒判定逻辑
- [x] VLM API 调用
- [x] TTS 语音播报

### Step 2: 调整 TTS 为女性声音（5 分钟）
- [ ] 设置 `pyttsx3` 为女性声音
- [ ] 测试语音播报效果

### Step 3: 添加本地 STT 支持（10 分钟，可选）
- [ ] 安装 `openai-whisper[tiny]`
- [ ] 实现简单的语音识别函数
- [ ] 集成到交互模式

### Step 4: 测试完整流程（10 分钟）
- [ ] 用本地视频测试跌倒检测
- [ ] 验证 VLM 响应
- [ ] 验证 TTS 播报
- [ ] 验证冷却机制

---

## 4. 后续开发计划（按优先级排序）

| 优先级 | 功能 | 预计时间 | 汇报价值 |
|--------|------|----------|----------|
| P0 | Gradio 可视化 Dashboard | 2h | ⭐⭐⭐⭐⭐ 领导最爱 |
| P1 | YOLOv8 CUDA 加速 | 1h | ⭐⭐⭐⭐ 技术深度 |
| P2 | 卡尔曼滤波 | 1.5h | ⭐⭐⭐⭐ 算法能力 |
| P3 | 多帧上传 + 速度分析 | 1h | ⭐⭐⭐ 精细化判断 |
| P4 | 本地 STT (Whisper tiny) | 0.5h | ⭐⭐ 交互体验 |
| P5 | 实时摄像头支持 | 0.5h | ⭐⭐ 实用性 |

---

## 5. 汇报策略 (For High Score)

### 5.1 核心故事线
1. **"我设计了一个端云协同的智能跌倒检测系统"**
   - 边缘端: YOLOv8 毫秒级检测
   - 云端: QVQ 百亿参数场景理解
   
2. **"为了演示，我用本地视频模拟实时流，但代码已支持摄像头输入"**
   - 展示代码中的注释部分
   - 说明只需一行改动即可切换

3. **"我做了很多工程优化"**
   - 时序滤波（防误报）
   - 冷却机制（防重复报警）
   - 异步 TTS（不阻塞视频）
   - Prompt 工程（场景化急救）

4. **"后续还有优化空间"** (展示技术深度)
   - 卡尔曼滤波（平滑关键点）
   - CUDA 加速（FPS 提升 3 倍）
   - Gradio Dashboard（可视化分析）

### 5.2 PPT 页面建议
- **页 1**: 项目背景（老龄化 + 跌倒危害）
- **页 2**: 系统架构图（边缘 + 云端）
- **页 3**: 技术栈（YOLOv8, QVQ, Python）
- **页 4**: 演示视频（跌倒检测 → 报警 → 语音建议）
- **页 5**: 技术亮点（时序滤波、异步架构）
- **页 6**: 后续优化（卡尔曼滤波、CUDA 加速）
- **页 7**: 总结与展望

---

## 6. 技术调研记录

### 6.1 YOLOv8 CUDA 加速
- **PyTorch CUDA 版本**: 需要 `torch>=2.0` + `CUDA 11.8/12.1`
- **安装命令**:
  ```bash
  pip install ultralytics
  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
  ```
- **验证 CUDA**: `torch.cuda.is_available()` 应返回 `True`
- **加速效果**: CPU ~15 FPS → GPU ~60 FPS (YOLOv8n)

### 6.2 Whisper Tiny 模型
- **模型大小**: 约 40MB
- **安装**: `pip install openai-whisper[tiny]`
- **推理速度**: ~1-2 秒/句（CPU），可接受
- **代码示例**:
  ```python
  import whisper
  model = whisper.load_model("tiny")
  result = model.transcribe("audio.wav")
  ```

### 6.3 TTS 女性声音设置
- **pyttsx3** 使用系统 TTS 引擎
- **Windows 默认**: "Microsoft Huihui Desktop"（女声）
- **代码**:
  ```python
  voices = engine.getProperty('voices')
  engine.setProperty('voice', voices[0].id)  # 通常是女声
  ```

---

## 7. 立即执行 (Action Items)

**当前任务**: 完成 MVP 核心功能调整（TTS 女声 + 测试）

**后续任务**（按时间排序）:
1. Gradio Dashboard 开发
2. YOLOv8 CUDA 加速
3. 卡尔曼滤波实现
4. 本地 STT 集成
5. 多帧上传优化

---

*Created by Trae AI Pair Programmer*
